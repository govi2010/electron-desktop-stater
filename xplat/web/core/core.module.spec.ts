import { ElectronStaterCoreModule } from './core.module';

describe('ElectronStaterCoreModule', () => {
  it('should work', () => {
    expect(new ElectronStaterCoreModule(null)).toBeDefined();
  });
});
